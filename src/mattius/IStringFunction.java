package mattius;

/* Interface StringFunction is being used as a "Functional Interface
 *    - only one method
 *    - acts like a function
 */
public interface IStringFunction {
    String constructString(String str);
}